apiVersion: v1
kind: Pod
metadata:
  name: crash-wrong-command
  labels:
    app: troubleshooting-demo
    scenario: crashloop
spec:
  containers:
  - name: web
    image: nginx
    command: ["/bin/invalid-command"]  # This command doesn't exist in the nginx image
    args: ["--invalid-arg"]
