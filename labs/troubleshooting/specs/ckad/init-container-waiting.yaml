apiVersion: v1
kind: Pod
metadata:
  name: init-container-waiting
  labels:
    app: troubleshooting-demo
    scenario: init-container
spec:
  initContainers:
  - name: wait-for-service
    image: busybox
    command: ["/bin/sh", "-c"]
    args:
    - |
      echo "Waiting for database service to be ready..."
      until nslookup nonexistent-database-service; do
        echo "Database service not ready yet, waiting..."
        sleep 5
      done
      echo "Database service is ready!"
  containers:
  - name: app
    image: nginx
    ports:
    - containerPort: 80
