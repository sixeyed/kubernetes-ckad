# This will fail validation - invalid engine
apiVersion: db.example.com/v1
kind: Database
metadata:
  name: invalid-engine
spec:
  engine: oracle  # Not in enum [postgres, mysql, mongodb, redis]
  version: "19.0"
  storageSize: 100Gi
