apiVersion: v1
kind: Pod
metadata:
  name: nginx-hostnetwork
  namespace: secure-app
  labels:
    app: nginx
spec:
  hostNetwork: true  # Violates baseline policy
  containers:
  - name: nginx
    image: nginx:alpine
    ports:
    - containerPort: 80
